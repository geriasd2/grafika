#include <math.h>
#define PI 3.14159265
/*
class Snail{
public:

    Point left_eye;
    Point right_eye;
    Point body;
    float eye_radius;
    float body_radius;

    Snail(Point left_eye, Point right_eye, Point body){
        this->left_eye = left_eye;
        this->right_eye = right_eye;
        this->body = body;
        this->eye_radius = 0.01f;
        this->body_radius = 0.1f;
    }

    void drawSnail() {
        float i = 0.0f;

        glBegin(GL_TRIANGLE_FAN);

        glVertex2f(this->body.x, this->body.y);
        for(i = 0.0f; i <= 360; i++)
            glVertex2f(body_radius*cos(PI * i / 180.0) + this->body.x, r*sin(PI * i / 180.0) + this->body.y);

        glEnd();
    }
};
*/
